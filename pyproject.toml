[project]
name = "rate-equation-dynamics"
version = "0.1.0"
description = "CTMC framework for entropy production in far-from-equilibrium systems."
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "jax[cuda12]",
    "jaxopt",
]
